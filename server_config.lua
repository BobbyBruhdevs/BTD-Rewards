Config = Config or {}

-- Server-only settings. Do not move these into config.lua.
Config.WebhookURL = ''
Config.AdminAce = 'btdrewards.admin'

-- Qbox group permissions. Values are minimum group grades.
Config.AdminGroups = {
	admin = 0,
	god = 0
}